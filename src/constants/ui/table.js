export const tableVariants = {
  default: "border border-[var(--border)]",

  striped: "border border-[var(--border)]",

  bordered: "border border-[var(--border)]"
};

export const tableSizes = {
  sm: "text-sm",

  md: "text-sm",

  lg: "text-base"
};
